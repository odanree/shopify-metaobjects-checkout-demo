{
  "name": "shopify-metaobjects-checkout-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "shopify app dev",
    "build": "shopify app build",
    "deploy": "shopify app deploy",
    "generate": "shopify app generate",
    "setup-metaobjects": "node scripts/create-metaobject-definitions.js",
    "webhook-server": "node app/server.js"
  },
  "workspaces": [
    "extensions/*"
  ],
  "dependencies": {
    "@shopify/shopify-api": "^9.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@shopify/cli": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
